// 不用另外引，默认已经引了
// 用于写整个站点的全局样式

body {
    margin: 0;
    font-size: 14px;
}

.breadcrumb {
   padding 1em 0
   margin 0 1.2em
   >.el-breadcrumb {
      font-size 12px
      a {
         font-weight normal
      }
   }
}